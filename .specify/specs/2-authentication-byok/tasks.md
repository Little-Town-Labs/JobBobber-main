# Task Breakdown — 2-authentication-byok

**Branch**: `2-authentication-byok`
**Plan**: `.specify/specs/2-authentication-byok/plan.md`
**Created**: 2026-02-24

**TDD Rule**: Every implementation task is blocked by its test task. No exceptions.

---

## Summary

| Metric                 | Value                                                 |
| ---------------------- | ----------------------------------------------------- |
| Total Tasks            | 19                                                    |
| Phases                 | 5                                                     |
| Total Estimated Effort | ~38 hours                                             |
| Critical Path Duration | ~28 hours (sequential)                                |
| Parallelizable         | Tasks 2.1, 3.1, 3.5 can start concurrently after T1.2 |

---

## Critical Path

```
T1.1 → T1.2 → T3.1 → T3.2 → T3.3 → T3.4 → T4.1 → T4.2 → T5.4 → T5.5
```

---

## Phase 1: Schema & Environment

### Task 1.1: Migration Verification Tests

**Status:** ✅ Completed
**Effort:** 1h
**Dependencies:** None
**Parallel with:** T2.1

**Description:**
Write an integration test that verifies the Prisma migration result. Tests must **FAIL** before the migration runs (no columns exist yet), then **PASS** after T1.2 completes.

**Acceptance Criteria:**

- [ ] Test checks `Employer` model has `byokApiKeyEncrypted`, `byokProvider`, `byokKeyValidatedAt`, `byokMaskedKey` fields (all nullable)
- [ ] Test checks `SeekerSettings` model has `byokMaskedKey` field (nullable)
- [ ] Tests confirmed to **FAIL** against current schema (pre-migration)
- [ ] Test file: `tests/integration/db-schema.test.ts` (or add to existing schema tests)

---

### Task 1.2: Extend Prisma Schema + Run Migration

**Status:** ✅ Completed (migration pending DB access; schema + Prisma client updated)
**Effort:** 1h
**Dependencies:** T1.1

**Description:**
Add BYOK fields to the Prisma schema and run the migration. All T1.1 tests must pass after this task.

**Acceptance Criteria:**

- [ ] `Employer` model in `prisma/schema.prisma` has:
  ```prisma
  byokApiKeyEncrypted String?
  byokProvider        String?
  byokKeyValidatedAt  DateTime?
  byokMaskedKey       String?
  ```
- [ ] `SeekerSettings` model has `byokMaskedKey String?` added
- [ ] Migration file generated by `pnpm db:migrate --name add_employer_byok_fields`
- [ ] Migration applied to dev DB; `pnpm db:generate` re-run to update Prisma client
- [ ] All T1.1 tests now **PASS**
- [ ] TypeScript compilation passes (`pnpm typecheck`)

---

## Phase 2: Middleware Gates

### Task 2.1: Middleware Unit Tests

**Status:** ✅ Completed
**Effort:** 2h
**Dependencies:** None (middleware reads JWT claims — no DB dependency)
**Parallel with:** T1.1

**Description:**
Write unit tests for the two sequential onboarding gates in `src/middleware.ts`. Mock `auth()` to return different `sessionClaims.metadata` combinations. Tests must **FAIL** against the current middleware.

**Test matrix** (all six cases must be tested):

| Clerk state                      | Target path       | Expected result                                  |
| -------------------------------- | ----------------- | ------------------------------------------------ |
| No session                       | /seeker/dashboard | Redirect to Clerk sign-in                        |
| Session, no role                 | /seeker/dashboard | Redirect to /onboarding/role                     |
| Session, no role                 | /onboarding/role  | Allow (gate destination)                         |
| Session, role set, no hasByokKey | /seeker/dashboard | Redirect to /setup/api-key                       |
| Session, role set, no hasByokKey | /setup/api-key    | Allow (gate destination)                         |
| Session, role + hasByokKey       | /seeker/dashboard | Allow through                                    |
| Session, role set                | /onboarding/role  | Redirect to /setup/api-key (re-entry prevention) |
| Session, role + hasByokKey       | /setup/api-key    | Redirect to role-specific dashboard              |

**Acceptance Criteria:**

- [ ] Test file: `tests/unit/middleware.test.ts`
- [ ] All 8 gate combinations tested
- [ ] Public routes (/, /sign-in, /sign-up, /api/trpc) exempt from gates
- [ ] Tests confirmed to **FAIL** against current middleware

---

### Task 2.2: Implement Middleware Gates

**Status:** ✅ Completed
**Effort:** 2h
**Dependencies:** T2.1

**Description:**
Extend `src/middleware.ts` with the two sequential onboarding gates after the existing Clerk `auth.protect()` call.

**Acceptance Criteria:**

- [ ] Gate 1: `sessionClaims.metadata.role === null` + not on `/onboarding/role` → `NextResponse.redirect('/onboarding/role')`
- [ ] Gate 2: `sessionClaims.metadata.hasByokKey !== true` + role set + not on `/setup/api-key` or `/onboarding/role` → `NextResponse.redirect('/setup/api-key')`
- [ ] Re-entry prevention: visiting `/onboarding/role` with role already set redirects to `/setup/api-key`
- [ ] Re-entry prevention: visiting `/setup/api-key` with both set redirects to role dashboard (`/seeker/dashboard` or `/employer/dashboard`)
- [ ] Public API routes (`/api/trpc`, `/api/inngest`, `/api/webhooks`) bypassed
- [ ] All T2.1 tests **PASS**
- [ ] ESLint passes (`pnpm lint`)

---

## Phase 3: tRPC Routers

### Task 3.1: `onboardingProcedure` Tests

**Status:** ✅ Completed
**Effort:** 1h
**Dependencies:** T1.2
**Parallel with:** T2.1 (can write while waiting for T1.2 if DB not needed for test stubs)

**Description:**
Write unit tests confirming `onboardingProcedure` enforces `userId` but allows `userRole: null`. Tests must **FAIL** before the new procedure is added to `src/server/api/trpc.ts`.

**Acceptance Criteria:**

- [ ] Test: procedure throws `UNAUTHORIZED` when `ctx.userId` is null
- [ ] Test: procedure allows through when `ctx.userId` is set and `ctx.userRole` is null
- [ ] Test: procedure allows through when both `ctx.userId` and `ctx.userRole` are set
- [ ] Test file: `tests/unit/server/api/trpc.test.ts` (or extend existing)
- [ ] Tests confirmed to **FAIL**

---

### Task 3.2: Implement `onboardingProcedure`

**Status:** ✅ Completed
**Effort:** 0.5h
**Dependencies:** T3.1

**Description:**
Add the new `onboardingProcedure` middleware tier to `src/server/api/trpc.ts`.

**Acceptance Criteria:**

- [ ] `enforceSession` middleware added:
  ```typescript
  const enforceSession = t.middleware(({ ctx, next }) => {
    if (!ctx.userId) throw new TRPCError({ code: "UNAUTHORIZED" })
    return next({ ctx: { ...ctx, userId: ctx.userId } })
  })
  export const onboardingProcedure = t.procedure.use(enforceSession)
  ```
- [ ] Exported from `trpc.ts` (ready for use by `onboardingRouter`)
- [ ] Existing `protectedProcedure` and sub-tiers unchanged
- [ ] All T3.1 tests **PASS**
- [ ] TypeScript compilation passes

---

### Task 3.3: `onboarding.setRole` Tests

**Status:** ✅ Completed
**Effort:** 3h
**Dependencies:** T1.2, T3.2

**Description:**
Write comprehensive unit tests for the `setRole` mutation. Mock Clerk SDK (`clerkClient`) and Prisma. Tests must **FAIL** before `onboarding.ts` is created.

**Test cases** (from plan.md testing strategy):

| Case                | What to verify                                                                                                 |
| ------------------- | -------------------------------------------------------------------------------------------------------------- |
| JOB_SEEKER path     | Role written to Clerk metadata; `JobSeeker` row created; `SeekerSettings` row created                          |
| EMPLOYER path       | Clerk Org created; role written; `Employer` row created; `EmployerMember` (ADMIN) created                      |
| Idempotency         | If `ctx.userRole` already set → returns `{ success: true, redirectTo: '/setup/api-key' }` without side effects |
| Orphan cleanup      | If DB write fails after Clerk org creation → `clerkClient.organizations.deleteOrganization` called             |
| Missing companyName | Throws `BAD_REQUEST` when role is EMPLOYER and companyName is absent/empty                                     |
| Invalid role        | Throws `BAD_REQUEST` for unknown role value                                                                    |
| No session          | Throws `UNAUTHORIZED` (onboardingProcedure enforces)                                                           |

**Acceptance Criteria:**

- [ ] Test file: `tests/unit/server/api/routers/onboarding.test.ts`
- [ ] All 7 test cases above covered
- [ ] Clerk SDK fully mocked (no real Clerk calls in unit tests)
- [ ] Prisma client mocked via vitest mock or test helper
- [ ] Tests confirmed to **FAIL**

---

### Task 3.4: Implement `onboarding.setRole`

**Status:** ✅ Completed
**Effort:** 3h
**Dependencies:** T3.3

**Description:**
Create `src/server/api/routers/onboarding.ts` with the `setRole` mutation. Register in `src/server/api/root.ts`.

**Acceptance Criteria:**

- [ ] `SetRoleInputSchema` from `contracts/trpc-contracts.ts` used for input validation
- [ ] `onboardingProcedure` used (not `protectedProcedure`)
- [ ] JOB_SEEKER path: `JobSeeker` + `SeekerSettings` created in Prisma `$transaction`; Clerk metadata updated
- [ ] EMPLOYER path: Clerk Org created first; if DB fails → org deleted; all in `$transaction`
- [ ] Idempotency: short-circuit if `ctx.userRole` already set; return `{ success: true, redirectTo: '/setup/api-key' }`
- [ ] Output matches `SetRoleOutputSchema` (literal `redirectTo: '/setup/api-key'`)
- [ ] Router added to `appRouter` in `root.ts` as `onboarding: onboardingRouter`
- [ ] All T3.3 tests **PASS**
- [ ] `pnpm typecheck` passes

---

### Task 3.5: `byok` Router Tests

**Status:** ✅ Completed
**Effort:** 4h
**Dependencies:** T1.2, T3.2
**Parallel with:** T3.3 (independent — different router)

**Description:**
Write unit tests for all four `byok` procedures: `storeKey`, `deleteKey`, `getKeyStatus` (and implicitly the self-healing sync logic). Mock Clerk SDK, Prisma, `src/lib/encryption.ts`, and `fetch` (provider validation). Tests must **FAIL** before `byok.ts` is created.

**Test cases** (from plan.md and contracts.ts):

| Procedure      | Case                                 | Expected                                                                                                               |
| -------------- | ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------- |
| `storeKey`     | Valid OpenAI key                     | Encrypted + stored in `SeekerSettings`/`Employer`; Clerk `hasByokKey=true`; returns `{ success, provider, maskedKey }` |
| `storeKey`     | Valid Anthropic key                  | Same flow for Anthropic path                                                                                           |
| `storeKey`     | Invalid key (provider 4xx)           | Throws `BAD_REQUEST`; nothing stored                                                                                   |
| `storeKey`     | Provider timeout (>10s)              | Throws `INTERNAL_SERVER_ERROR`; nothing stored                                                                         |
| `storeKey`     | Provider rate limit (429)            | Throws `BAD_REQUEST` with rate-limit message                                                                           |
| `storeKey`     | OpenAI key for Anthropic provider    | Throws `BAD_REQUEST` (Zod `superRefine`)                                                                               |
| `storeKey`     | Response has no `apiKey` field       | Assert output schema never contains key material                                                                       |
| `storeKey`     | No role (FORBIDDEN)                  | Throws `FORBIDDEN`                                                                                                     |
| `deleteKey`    | JOB_SEEKER path                      | `byokApiKeyEncrypted=null`, `byokMaskedKey=null`; Clerk `hasByokKey=false`                                             |
| `deleteKey`    | EMPLOYER path                        | Same for employer                                                                                                      |
| `deleteKey`    | No key stored                        | No-op; returns `{ success: true }`                                                                                     |
| `getKeyStatus` | Key present                          | Returns `{ hasKey: true, provider, maskedKey }` — no ciphertext                                                        |
| `getKeyStatus` | No key                               | Returns `{ hasKey: false, provider: null, maskedKey: null }`                                                           |
| `getKeyStatus` | DB has key, Clerk `hasByokKey=false` | Self-healing: Clerk metadata re-synced to `true`; returns key status                                                   |

**Acceptance Criteria:**

- [ ] Test file: `tests/unit/server/api/routers/byok.test.ts`
- [ ] All 14 test cases above covered
- [ ] `fetch` mocked globally via Vitest (`vi.stubGlobal('fetch', ...)`)
- [ ] `src/lib/encryption.ts` mocked to avoid real crypto in unit tests
- [ ] Tests confirmed to **FAIL**

---

### Task 3.6: Implement `byok` Router

**Status:** ✅ Completed
**Effort:** 4h
**Dependencies:** T3.5

**Description:**
Create `src/server/api/routers/byok.ts` with all four procedures. Register in `src/server/api/root.ts`.

**Acceptance Criteria:**

- [ ] `ByokStoreKeyInputSchema` from contracts used for `storeKey` input
- [ ] Provider validation uses native `fetch` + `AbortController` (10s timeout):
  - OpenAI: `GET https://api.openai.com/v1/models` with `Authorization: Bearer <key>`
  - Anthropic: `GET https://api.anthropic.com/v1/models` with `x-api-key: <key>` + `anthropic-version: 2023-06-01`
- [ ] On 4xx from provider → throw `BAD_REQUEST` with provider-friendly message
- [ ] On timeout/AbortError → throw `INTERNAL_SERVER_ERROR`
- [ ] On 429 → throw `BAD_REQUEST` with rate-limit message
- [ ] Encryption via `encrypt(apiKey, ctx.userId)` from `src/lib/encryption.ts`
- [ ] Storage branch: `JOB_SEEKER` → `SeekerSettings.update`; `EMPLOYER` → `Employer.update` where `clerkOrgId = ctx.orgId`
- [ ] Both `byokApiKeyEncrypted` and `byokMaskedKey` written in same DB update
- [ ] Clerk metadata updated (`hasByokKey: true`) after DB write (best-effort)
- [ ] `getKeyStatus` self-healing: if DB has key but Clerk metadata `hasByokKey=false`, re-sync Clerk metadata before returning
- [ ] `deleteKey` is a no-op if no key stored (no error thrown)
- [ ] Response objects match output schemas from contracts
- [ ] Router added to `appRouter` in `root.ts` as `byok: byokRouter`
- [ ] All T3.5 tests **PASS**

---

### Task 3.7: Security Review — tRPC Routers

**Status:** ✅ Completed (no CRITICAL; H-3 addressed via comment fix + max length; M-3 logging added)
**Effort:** 1h
**Dependencies:** T3.4, T3.6

**Description:**
Run `/security-review` on the two new routers and the updated `trpc.ts`. Focus on BYOK key material handling and Clerk SDK calls.

**Acceptance Criteria:**

- [ ] No raw `apiKey` value in any log line, Sentry breadcrumb, or error message
- [ ] `byokApiKeyEncrypted` never selected in any query result returned to tRPC caller
- [ ] No hardcoded secrets or API endpoints (provider URLs are constants, not user-provided)
- [ ] OWASP: input validation present (Zod); no SQL injection (Prisma parameterised); no SSRF (hardcoded URLs)
- [ ] All CRITICAL/HIGH issues from security review resolved before proceeding to Phase 4

---

## Phase 4: UI Pages

### Task 4.1: Role Selection Component Tests

**Status:** 🔴 Blocked by T3.4
**Effort:** 2h
**Dependencies:** T3.4

**Description:**
Write React Testing Library unit tests for `RoleSelectorForm` client component. Mock `trpc.onboarding.setRole.useMutation`. Tests must **FAIL** before the component is created.

**Acceptance Criteria:**

- [ ] Test file: `tests/unit/components/onboarding/role-selector.test.tsx`
- [ ] Tests: renders two role cards ("I'm looking for work", "I'm hiring")
- [ ] Tests: selecting "I'm hiring" reveals company name input
- [ ] Tests: selecting "I'm looking for work" hides company name input
- [ ] Tests: submit with empty company name for EMPLOYER shows validation error
- [ ] Tests: submit calls mutation with correct payload
- [ ] Tests: loading spinner visible while mutation is pending
- [ ] Tests: error `Alert` shown on mutation failure
- [ ] Tests confirmed to **FAIL**

---

### Task 4.2: Implement Onboarding Layout + Role Page

**Status:** 🔴 Blocked by T4.1
**Effort:** 3h
**Dependencies:** T4.1

**Description:**
Create the onboarding route group layout and the role selection page.

**Files to create:**

- `src/app/(onboarding)/layout.tsx`
- `src/app/(onboarding)/onboarding/role/page.tsx`
- `src/components/onboarding/role-selector.tsx`

**Acceptance Criteria:**

- [ ] `layout.tsx`: Server Component; centered full-page shell; no sidebar; "Step 1 of 2" indicator; JobBobber logo
- [ ] `page.tsx` (role): Server Component; reads `auth()` session claims; if role already set → `redirect('/setup/api-key')`; else renders `<RoleSelectorForm>`
- [ ] `role-selector.tsx`: Client Component with two shadcn/ui `Card` options; company name `Input` reveals on Employer selection; submit via `trpc.onboarding.setRole.useMutation()`; on success → `router.push(result.redirectTo)`
- [ ] Error state: `Alert` with destructive variant for mutation errors
- [ ] Loading state: button shows spinner; inputs disabled during `isPending`
- [ ] All T4.1 tests **PASS**
- [ ] `pnpm typecheck` passes

---

### Task 4.3: BYOK Setup Form Tests

**Status:** 🔴 Blocked by T3.6
**Effort:** 2h
**Dependencies:** T3.6
**Parallel with:** T4.1 (can run concurrently — different component)

**Description:**
Write React Testing Library tests for `ByokSetupForm` client component. Mock `trpc.byok.storeKey.useMutation`. Tests must **FAIL** before the component is created.

**Acceptance Criteria:**

- [ ] Test file: `tests/unit/components/onboarding/api-key-form.test.tsx`
- [ ] Tests: renders provider radio group (OpenAI / Anthropic)
- [ ] Tests: cost estimate `Alert` visible on load
- [ ] Tests: key format hint updates based on provider selection
- [ ] Tests: submit calls mutation with `{ provider, apiKey }`
- [ ] Tests: loading state shown during `isPending`
- [ ] Tests: error `Alert` shown on mutation failure with provider-specific message
- [ ] Tests: on success → `router.push('/seeker/dashboard')` or `/employer/dashboard` based on session role
- [ ] Tests confirmed to **FAIL**

---

### Task 4.4: Implement BYOK Setup Page

**Status:** 🔴 Blocked by T4.3
**Effort:** 2.5h
**Dependencies:** T4.3

**Description:**
Create the BYOK setup page and form component.

**Files to create:**

- `src/app/(onboarding)/setup/api-key/page.tsx`
- `src/components/onboarding/api-key-form.tsx`

**Acceptance Criteria:**

- [ ] `page.tsx` (setup): Server Component; reads `hasByokKey` from session claims; if `true` → `redirect` to role dashboard; else renders `<ByokSetupForm userRole={role}>`
- [ ] `api-key-form.tsx`: Client Component; `RadioGroup` for OpenAI/Anthropic; cost estimate `Alert`; `Input` with `type="password"`; key format hint below input; submit via `trpc.byok.storeKey.useMutation()`; on success → redirect to role dashboard
- [ ] Accessibility: `Label` associated with inputs; error `Alert` has `role="alert"` / `aria-live="polite"`; full keyboard navigation
- [ ] All T4.3 tests **PASS**
- [ ] `pnpm typecheck` passes

---

### Task 4.5: Account Key Management Tests

**Status:** 🔴 Blocked by T3.6
**Effort:** 1.5h
**Dependencies:** T3.6
**Parallel with:** T4.1, T4.3

**Description:**
Write tests for `ApiKeyManager` client component and the account key settings page. Mock `trpc.byok.getKeyStatus.useQuery`, `trpc.byok.deleteKey.useMutation`, `trpc.byok.storeKey.useMutation`. Tests must **FAIL** before components are created.

**Acceptance Criteria:**

- [ ] Test file: `tests/unit/components/onboarding/api-key-manager.test.tsx`
- [ ] Tests: renders provider name + masked key (e.g., "OpenAI — sk-...xxxx")
- [ ] Tests: "Replace Key" reveals inline `ByokSetupForm`
- [ ] Tests: "Delete Key" opens `AlertDialog` confirmation
- [ ] Tests: confirming delete calls `trpc.byok.deleteKey.useMutation()`
- [ ] Tests: after delete, user sees "Key removed" state; AI features disabled message shown
- [ ] Tests confirmed to **FAIL**

---

### Task 4.6: Implement Account Key Settings Page

**Status:** 🔴 Blocked by T4.5
**Effort:** 2h
**Dependencies:** T4.5

**Description:**
Create the account API key settings page and management component.

**Files to create:**

- `src/app/account/api-key/page.tsx`
- `src/components/onboarding/api-key-manager.tsx`

**Acceptance Criteria:**

- [ ] `page.tsx`: Server Component; fetches key status via tRPC server caller; passes `{ hasKey, provider, maskedKey }` to `<ApiKeyManager>`
- [ ] `api-key-manager.tsx`: Client Component; shows provider + masked key; "Replace Key" button reveals inline `<ByokSetupForm>` (reused component); "Delete Key" → `AlertDialog` confirmation → `trpc.byok.deleteKey.useMutation()`; post-delete state shows disabled AI features message
- [ ] Raw key value must not appear anywhere in component state or rendered output
- [ ] All T4.5 tests **PASS**
- [ ] `pnpm typecheck` passes

---

## Phase 5: Security Hardening & Quality Gates

### Task 5.1: Security Hardening

**Status:** 🔴 Blocked by T3.7, T4.2, T4.4, T4.6
**Effort:** 2h
**Dependencies:** T3.7, T4.2, T4.4, T4.6

**Description:**
Apply the security measures from plan.md Phase 5.

**Acceptance Criteria:**

- [ ] **Log guards**: tRPC error formatter strips `apiKey` and `byokApiKeyEncrypted` from error `data` objects
- [ ] **Input whitespace**: `z.string().trim()` applied in `RawApiKeySchema` (contracts define this)
- [ ] **AbortController timeout**: 10-second provider fetch timeout implemented and tested (covered by T3.5)
- [ ] **Accessibility audit**: all onboarding forms pass basic a11y checks (keyboard nav, ARIA labels, error announcements); use `@testing-library/jest-dom` accessibility assertions in T4.1 / T4.3 test suites
- [ ] **No `console.log`**: run `pnpm lint`; confirm zero console.log warnings in new files

---

### Task 5.2: Integration Tests

**Status:** 🔴 Blocked by T3.4, T3.6
**Effort:** 3h
**Dependencies:** T3.4, T3.6

**Description:**
Write integration tests using Vitest + real DB (test DB from Feature 1 setup). These tests hit the actual Prisma client but mock Clerk and external fetch.

**Test cases** (from plan.md):

- `setRole` (JOB_SEEKER): verifies `JobSeeker` + `SeekerSettings` rows created with correct foreign keys
- `setRole` (EMPLOYER): verifies `Employer` + `EmployerMember` rows; mocked Clerk Org ID stored correctly
- `storeKey` → `getKeyStatus` round-trip: key stored; `getKeyStatus` returns `hasKey: true`; raw key not in response
- `deleteKey` → `getKeyStatus`: `hasKey: false`; DB row is null

**Acceptance Criteria:**

- [ ] Test file: `tests/integration/onboarding.test.ts`
- [ ] All 4 integration scenarios pass
- [ ] Uses real Prisma client against test DB (`DATABASE_URL` from test env)
- [ ] Clerk SDK mocked (no real Clerk API calls in CI)
- [ ] Each test rolls back DB changes via `afterEach` cleanup

---

### Task 5.3: E2E Tests

**Status:** 🔴 Blocked by T4.2, T4.4, T4.6
**Effort:** 3h
**Dependencies:** T4.2, T4.4, T4.6

**Description:**
Write Playwright E2E tests covering the critical onboarding flows from the quickstart validation guide.

**Test flows** (from plan.md and quickstart.md):

1. Job Seeker full onboarding (sign-up → role select → BYOK setup → dashboard)
2. Employer full onboarding (sign-up → role + company name → BYOK setup → dashboard)
3. Invalid API key rejection (error shown; DB unchanged)
4. Gate bypass attempts (navigate directly to dashboard without completing steps)
5. Key replacement (account settings → replace key → success)
6. Key deletion (account settings → delete → redirected to /setup/api-key)
7. Role enforcement (job seeker can't access employer routes → 403/redirect)

**Acceptance Criteria:**

- [ ] Test file: `tests/e2e/onboarding.spec.ts`
- [ ] All 7 flows covered
- [ ] Tests run against the dev server (`pnpm dev`) or a Playwright staging environment
- [ ] Screenshots captured for all failure states
- [ ] Tests pass in CI (`pnpm test:e2e`)

---

### Task 5.4: Final Code Review

**Status:** 🔴 Blocked by T5.1, T5.2, T5.3
**Effort:** 1h
**Dependencies:** T5.1, T5.2, T5.3

**Description:**
Run `/code-review` on all new and modified files. Address all CRITICAL and HIGH issues before closing the feature.

**Files to review:**

- `src/middleware.ts`
- `src/server/api/trpc.ts`
- `src/server/api/routers/onboarding.ts`
- `src/server/api/routers/byok.ts`
- `src/server/api/root.ts`
- `src/app/(onboarding)/layout.tsx`
- `src/app/(onboarding)/onboarding/role/page.tsx`
- `src/app/(onboarding)/setup/api-key/page.tsx`
- `src/app/account/api-key/page.tsx`
- `src/components/onboarding/*.tsx`
- `prisma/schema.prisma`

**Acceptance Criteria:**

- [ ] Zero CRITICAL issues
- [ ] Zero HIGH issues
- [ ] All MEDIUM issues reviewed and accepted or fixed
- [ ] `pnpm build` succeeds (zero TypeScript errors, zero ESLint errors)
- [ ] `pnpm test` passes (80%+ coverage on new files)

---

## Dependency Graph

```
T1.1 ──────────────────────────────────────────────────────────────────┐
T1.2 (blocked: T1.1) ────────┬──────────────────┬──────────┬──────────┤
T2.1 (parallel: T1.1) ─┐     │                  │          │          │
T2.2 (blocked: T2.1) ──┘     │                  │          │          │
                         T3.1─┤             T3.5─┤          │          │
                         T3.2─┤             T3.6─┤          │          │
                         T3.3─┤                  │          │          │
                         T3.4─┤──T4.1─T4.2───────┤          │          │
                         T3.7─┤             T4.3─T4.4       │          │
                               │             T4.5─T4.6       │          │
                               └──T5.2─────────────┐         │          │
                                    T5.1────────────┤         │          │
                                    T5.3────────────┤         │          │
                                    T5.4 (final) ◄──┘         │          │
```

---

## Parallelization Windows

| Window   | Tasks that can run in parallel | Prerequisite                      |
| -------- | ------------------------------ | --------------------------------- |
| Window A | T1.1 and T2.1                  | None                              |
| Window B | T3.1, T3.5                     | T1.2 complete                     |
| Window C | T3.3 and T3.5                  | T3.2 complete                     |
| Window D | T4.1, T4.3, T4.5               | T3.4 or T3.6 (respectively)       |
| Window E | T5.1, T5.2, T5.3               | T3.7, T4.2/4.4/4.6 (respectively) |

---

## Task Status Legend

| Symbol         | Meaning                                         |
| -------------- | ----------------------------------------------- |
| 🟡 Ready       | No blocking dependencies; can start immediately |
| 🔴 Blocked     | Waiting on listed tasks                         |
| 🟢 In Progress | Currently being worked                          |
| ✅ Complete    | All acceptance criteria met                     |
